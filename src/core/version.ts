/**
 * Build-stamped version constants. Edit `version` for releases; CI asserts
 * this matches the git tag.
 */
export const SlopguardVersion = {
  version: "0.1.0",
  toolName: "slopguard-typescript",
} as const;
